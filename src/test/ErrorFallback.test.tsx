// import { render, screen, waitFor } from '@testing-library/react';
import { describe, it } from 'vitest';
// import App from '../App';
// import { userEvent } from '@testing-library/user-event';

describe('Testting ErrorFallback component', () => {
  // it('Should render fallback UI after throwing and error', async () => {
  //   render(<App />);
  //   const btn = screen.getByText('Throw an error!');
  //   userEvent.click(btn);
  //   await new Promise((res) => setTimeout(res, 1000));
  //   waitFor(() => {
  //     expect(
  //       screen.getByText('Something bad have happened :(')
  //     ).toBeInTheDocument();
  //   });
  // });
  it('stub', () => {});
});
